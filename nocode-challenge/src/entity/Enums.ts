export enum FeatureApplicability {
    REQUIRED = "required",
    OPTIONAL = "optional",
    INAPPLICABLE = "inapplicable"
}

export enum UnitSystem {
    SI = "si",
    IMPERIAL = "imperial"
}

export enum ApprovalStatus {
    APPROVED = "approved",
    REJECTED = "rejected",
    REVIEW = "review"
}