{
   "type": "postgres",
   "host": "localhost",
   "port": 5432,
   "username": "articledb_challenge", adapt this
   "password": "challenge", adapt this
   "database": "articledb_challenge", adapt this
   "synchronize": true,
   "logging": false,
   "entities": [
      "src/entity/**/*.ts"
   ],
   "migrations": [
      "src/migration/**/*.ts"
   ],
   "subscribers": [
      "src/subscriber/**/*.ts"
   ],
   "cli": {
      "entitiesDir": "src/entity",
      "migrationsDir": "src/migration",
      "subscribersDir": "src/subscriber"
   }
}
